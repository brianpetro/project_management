<div class="row">
		<p id="notice"><%= notice %></p>	
			
		<!-- Add task if has project_id -->
			

		
		<% if current_user.is_reviewer_of_project?(@project.id) %>
			<div class="span8">
				<%= render 'review' %>
			</div>
			<div class="span4">
				<%= render 'reviewer-sidebar' %>
			</div>
		<% end %>
		<!-- Begin visible to project participants only, NEEDS request to participate in ELSE -->
		<% if current_user.is_participant?(@project.id) %>
			<%= render 'participant' %>
		<% end %> <!-- End project participant only -->

</div>
