<h1>Listing notes</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Note</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @notes.each do |note| %>
  <tr>
    <td><%= note.id %></td>
    <td><%= note.note %></td>
    <td><%= link_to 'Show', [@project, note] %></td>
    <td><%= link_to 'Edit', edit_project_note_path(@project, note) %></td>
    <td><%= link_to 'Destroy', [@project, note], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Note', new_project_note_path %>
