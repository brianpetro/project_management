<%= simple_form_for(@blueprint) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :id %>
    <%= f.input :loc %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
