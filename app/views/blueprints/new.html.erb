<% if params[:type] == 'discussion' %>
	<h1>New Discussion</h1>
<% else %>
	<h1>New Blueprint</h1>
<% end %>
<%= render 'form' %>

<%= link_to 'Back', project_path(@project), class: 'btn btn-small' %>
